import { ImpactReport, EstimationParams } from "./model";
export declare class EstimationParser {
    private quick;
    parseReport(report: ImpactReport): EstimationParams;
    private parseHasWebsite(hasWebsite);
}
