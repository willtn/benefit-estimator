{
  "name": "benefit-estimator",
  "version": "1.0.2",
  "description": "Benefit Estimator for Student Benfit",
  "main": "dist/index.js",
  "scripts": {
    "build": "rimraf build/**/* && webpack -p --progress --colors && dts-bundle --name benefit-estimator --main build/index.d.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "release": "rimraf dist/**/* && npm run build && cp -r build/* dist/"
  },
  "repository": {
    "type": "git",
    "url": "git@gitlab.pdx.groowin.com:trung.nguyen/benefit-estimator.git"
  },
  "author": "Trung Nguyen",
  "private": true,
  "devDependencies": {
    "dts-bundle": "^0.7.3",
    "rimraf": "^2.6.1",
    "ts-loader": "^2.3.4",
    "typescript": "^2.6.1",
    "webpack": "^3.5.5"
  }
}
